{
  "name": "subcrypt",
  "version": "1.0.0",
  "main": "dist/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --coverage"
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "jest": {
    "preset": "ts-jest",
    "transform": {
      "^.+\\.(ts|tsx)?$": "ts-jest",
      "^.+\\.(js|jsx)$": "babel-jest"
    },
    "collectCoverage": true,
    "coverageThreshold": {
      "global": {
        "lines": 100
      }
    }
  },
  "devDependencies": {
    "@babel/preset-env": "^7.28.3",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.5.2",
    "babel-jest": "^30.1.2",
    "jest": "^30.1.3",
    "ts-jest": "^29.4.4",
    "typescript": "^5.9.2"
  }
}
